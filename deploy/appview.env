# Appview Service Config
NODE_ENV=production
BSKY_VERSION=1.0.0
BSKY_PORT=3000
BSKY_SERVER_DID=did:web:socialapp.nexthouse.org
BSKY_ADMIN_PASSWORDS=96+WZkL6DxTIH0h0P2z  # Admin password(s), should match your setup
BSKY_BLOB_CACHE_LOC=/root/BskyAppViewLiteData/blob-cache

# Connections
BSKY_PUBLIC_URL=https://socialapp.nexthouse.org  # Public URL of the appview (replace with your actual URL)
BSKY_DID_PLC_URL=https://plc.directory         # The URL of the PLC service for DID document resolution
BSKY_DATAPLANE_URL=http://localhost:2583    # The URL to the PDS service
BSKY_BSYNC_URL=http://localhost:2583        # Bsync URL for syncing
PLC_HOST=https://plc.directory               # URL for the PLC (DID resolution)
BSKY_DATAPLANE_URLS=http://localhost:2583


# Moderation (For handling content moderation)
MOD_SERVICE_DID=did:web:bsky.social         # DID for the moderation service

# Signing key for API communication (service-to-service authentication)
BSKY_SERVICE_SIGNING_KEY=c7f567fe2b5a735983a14cc05c3b13473f33f0dd7669285dbfb044872b0001bf

# Federation Allowlist (specify repositories allowed to interact with the service)
REPO_ACCEPT_LIST=https://socialpds.nexthouse.org

# Open registration (allows users to register via Appview)
APPVIEW_OPEN_REGISTRATION=true

# Optional - Image resizing and blob storage configurations
#BSKY_IMG_URI_ENDPOINT=https://cdn.bsky.app/img  # Base URL for resized images
#BSKY_BLOB_CACHE_LOC=/root/BskyAppViewLiteData/blob-cache  # Cache location for resized images

# Handle resolution - DNS servers to use for handle resolution (comma-separated)
BSKY_HANDLE_RESOLVE_NAMESERVERS=1.1.1.1,8.8.8.8  # DNS servers for handle resolution

# Courier Service (optional, if using a courier service for notifications)
#BSKY_COURIER_URL=https://your-courier-service-url  # Provide the actual URL for the courier service
#BSKY_COURIER_API_KEY=your-courier-api-key         # API key for the courier service

# Search Service (optional, if you're using a custom search service)
#BSKY_SEARCH_URL=http://search-service-url  # Set URL for the search service if using a custom one
